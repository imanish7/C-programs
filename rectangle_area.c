#include<stdio.h>
main()
{
	int length,breadth;
	int area;
	printf("\nEnter value of length");
	scanf("%d",&length);
	printf("\nEnter the value of breadth");
	scanf("%d",&breadth);
	area= length*breadth;
	printf("\nArea of rectangle is %d",area);
}
